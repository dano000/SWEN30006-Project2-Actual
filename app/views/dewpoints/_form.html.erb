<%= form_for(@dewpoint) do |f| %>
  <% if @dewpoint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dewpoint.errors.count, "error") %> prohibited this dewpoint from being saved:</h2>

      <ul>
      <% @dewpoint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :current %><br>
    <%= f.text_field :current %>
  </div>
  <div class="field">
    <%= f.label :weather_id %><br>
    <%= f.text_field :weather_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
